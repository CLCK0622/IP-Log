{
  "name": "iplog-project",
  "version": "1.0.0",
  "description": "A project to log IP addresses and referrer URLs.",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "local-dev": "vercel dev"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "vercel": "^37.12.1"
  }
}
